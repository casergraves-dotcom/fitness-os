import type {
  CapacitorConfig,
} from "@capacitor/cli";

const config: CapacitorConfig = {
  appId:
    "com.casergraves.fitnessos",

  appName:
    "Fitness OS",

  webDir:
    "out",
};

export default config;
